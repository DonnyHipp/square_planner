name: Deploy Docker Stack

on:
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Check
        run: echo ${{ vars.REMOTE_HOST }}

      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v1

      - name: Create SSH Key
        run: |
          echo "${{ secrets.SSH_PRIVATE_KEY }}" > ./id_rsa
          chmod 600 ./id_rsa

      - name: Add Remote Host to Known Hosts
        run: |
          mkdir -p ~/.ssh
          ssh-keyscan -H ${{ vars.REMOTE_HOST }} >> ~/.ssh/known_hosts

      - name: Build Docker images
        env:
          IMAGE_TAG: ${{vars.REPO_NAME}}:${{github.head_ref}}
          CI_REGISTRY_BACKEND : ${{ vars.CR_REGISTRY }}
        run: |
          echo $CI_REGISTRY_BACKEND
          echo IMAGE_TAG
          docker compose build

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          registry: ${{ secrets.CR_REGISTRY }}
          username: ${{ secrets.CR_KEY_ID }}
          password: ${{ secrets.CR_KEY_PASS }}

      - name: Push Docker images
        uses: docker/build-push-action@v5

        with:
          push: true
          context: .
          tags: ${{vars.REPO_NAME}}:${{github.head_ref}}

      - name: Deploy
        uses: kitconcept/docker-stack-deploy@v1.0.1
        with:
            registry: ${{ vars.CR_REGISTRY }}
            username: ${{ secrets.CR_KEY_ID }}
            password: ${{ secrets.CR_KEY_PASS }}
            remote_host: ${{ vars.REMOTE_HOST }}
            remote_user: ${{ vars.REMOTE_USER }}
            remote_private_key: ${{ secrets.REMOTE_PRIVATE_KEY }}
            stack_file: "docker-compose.yml"
            stack_name: "sq"



#      - name: Deploy Docker Stack on Remote Server
#        run: |
#          ssh -i ./id_rsa ${{ secrets.REMOTE_USER }}@${{ secrets.REMOTE_HOST }} << 'EOF'
#            docker stack deploy -c /path/to/your/docker-compose.yml your_stack_name
#          EOF
