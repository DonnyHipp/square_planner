services:

  backend:
    build: ./back
    image: ${CI_REGISTRY_BACKEND}/${IMAGE_TAG}
    ports:
      - "8080:8080"

  nginx:
    image: nginx:latest
    ports:
      - "80:80"
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf
    depends_on:
      - backend
